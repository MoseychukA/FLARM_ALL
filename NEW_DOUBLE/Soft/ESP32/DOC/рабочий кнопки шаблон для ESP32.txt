Отлично, такой подход — оптимальный для ESP32:

ISR только уведомляет: "на кнопке было изменение" (и запоминает текущее состояние + millis());
Вся логика дебаунса, таймингов первого/второго клика, long — в зада
Да, такой подход (ISR = только фиксация момента и состояния, вся остальная обработка целиком в задаче) является наиболее устойчивым для ESP32.

Ниже — рабочий пример для ESP32/ESP32-S3: ISR собирает факты смены состояния и кладёт их в небольшой буфер 
(или просто в структуру флагов и времени), а вся дебаунс‑логика, разбора LONG/SHORT/DOUBLE реализуется в RTOS‑задаче на отдельном ядре.

Пример
Вот оптимальное решение для ESP

Отличный подход! В этом случае ISR (прерывание) очень коротко только ставит флаг «КНОПКА ИЗМЕНЕНА» и отмечает время.
Вся логика таймингов (дебаунс, окна двойного клика, long press…) — в задаче ButtonTask.

Вот рабочий шаблон для ESP32:

1. Краткая задача и ISR
Глобальные переменные:

const int BUTTON_PIN = 45;

// Флаги для задачи и ISR
volatile bool btn_event = false;
volatile unsigned long btn_event_time = 0;
volatile bool btn_state_isr = true; // true = отпущена

// Для debounce inside ISR
const unsigned long ISR_DEBOUNCE_MS = 15;
volatile unsigned long last_isr_time = 0;
ISR:

void IRAM_ATTR onButtonChange() {
  unsigned long tnow = millis();
  if (tnow - last_isr_time < ISR_DEBOUNCE_MS) return;
  last_isr_time = tnow;
  btn_state_isr = digitalRead(BUTTON_PIN); // true/false
  btn_event = true; // просто сигнал задачи "проверь кнопку!"
  btn_event_time = tnow;
}
В setup() только attachInterrupt:

pinMode(BUTTON_PIN, INPUT_PULLUP);
attachInterrupt(digitalPinToInterrupt(BUTTON_PIN), onButtonChange, CHANGE);
2. ButtonTask() (логика таймингов, двойных кликов и long-press)
void ButtonTask(void* pvParameters)
{
  const uint16_t LOGIC_DEBOUNCE_MS = 35; // программный антидребезг
  const uint16_t DOUBLE_WIN_MS = 350;
  const uint16_t LONG_PRESS_MS = 2000;

  bool stable = true;
  bool lastStable = true;
  unsigned long lastChangeMs = millis();
  unsigned long pressStartMs = 0;

  bool waitingSecond = false;
  unsigned long secondWinEndMs = 0;
  bool longReported = false;

  const TickType_t tick = pdMS_TO_TICKS(5);

  for (;;)
  {
    // Проверяем был ли event из ISR
    if (btn_event) {
      btn_event = false; // сбросить СРАЗУ чтобы не потерять следующее прерывание

      bool now_raw = btn_state_isr; // HIGH = отпущена, LOW = нажата
      unsigned long tnow = btn_event_time;

      if (now_raw != stable && (tnow - lastChangeMs >= LOGIC_DEBOUNCE_MS)) {
        lastStable = stable;
        stable = now_raw;
        lastChangeMs = tnow;

        // --- Ниже ровно такой же логический автомат, как в чисто-поллинг варианте ---

        if (lastStable == true && stable == false) // отпущена → нажата
        {
          pressStartMs = tnow;
          longReported = false;
        }
        else if (lastStable == false && stable == true) // нажата → отпущена
        {
          unsigned long dur = tnow - pressStartMs;
          if (dur >= LONG_PRESS_MS && !longReported)
          {
            AuxData.new_buttton_M = 3;  // LONG
            waitingSecond = false;
          }
          else
          {
            if (waitingSecond)
            {
              AuxData.new_buttton_M = 2; // DOUBLE_SHORT
              waitingSecond = false;
            }
            else
            {
              waitingSecond = true;
              secondWinEndMs = tnow + DOUBLE_WIN_MS;
            }
          }
        }
      }
      // иначе — событие было, но игнорировано по debounce
    }
    else
    {
      // Нет нового события из ISR — следим за long hold
      unsigned long tnow = millis();
      if (stable == false && !longReported && (tnow - pressStartMs >= LONG_PRESS_MS))
      {
        longReported = true;
        AuxData.new_buttton_M = 3;     // LONG
        waitingSecond = false;
      }
      // Истечение окна double-click
      if (waitingSecond && ((int32_t)(tnow - secondWinEndMs) >= 0))
      {
        waitingSecond = false;
        AuxData.new_buttton_M = 1;  // SHORT
      }
    }
    vTaskDelay(tick); // 5 мс
 
Да, это возможно и достаточно стабильный подход:
ISR делает только минимум — фиксирует факт изменения (и желательно save millis()/состояние в volatile переменную).
Вся обработка таймингов: дебаунс, двойной, длинный клик — делается в RTOS‑задаче на основе этих флагов/меток времени.

Пример полностью устойчивой схемы:
1. ISR (фиксируем только изменения)
volatile bool buttonEvent = false;
volatile bool buttonPhysState = true;  // true — отпущена (INPUT_PULLUP), false — нажата
volatile unsigned long buttonISRTimestamp = 0;

IRAM_ATTR void onButtonChange() {
  buttonPhysState = (digitalRead(BUTTON_PIN) == HIGH); // true — отпущена
  buttonISRTimestamp = millis();                       // на ESP32 millis работает в ISR, но не идеально
  buttonEvent = true;
}
2. В setup()
pinMode(BUTTON_PIN, INPUT_PULLUP);
attachInterrupt(digitalPinToInterrupt(BUTTON_PIN), onButtonChange, CHANGE);
3. Задача-обработчик (RTOS task в цикле)
void ButtonTask(void* param)
{
  const uint32_t DEBOUNCE_MS   = 35;
  const uint32_t DOUBLE_WIN_MS = 350;
  const uint32_t LONG_MS       = 2000;
  bool lastStableState = true;
  bool stableState = true;
  uint32_t lastChangeTime = 0;
  uint32_t pressTime = 0;

  bool waitingSecondClick = false;
  uint32_t doubleWindowEnd = 0;
  bool longReported = false;

  for(;;)
  {
    static bool lastISRState
Вот полностью рабочий пример для ESP32/ESP32S3 (Arduino) ―
быстрая реакция на клик через ISR, но вся логика антидребезга, двойного/длинного нажатия ― В ЗАДАЧЕ, а не в ISR.

Кратко:

ISR только фиксирует “сырое” изменение и кладёт его во флаг + timestamp (никаких millis, никаких вычислений таймингов)
В ButtonTask (основная реализация FSM ― finite state machine) — читаем сырое значение только когда изменился, обсчитываем debounce, double click и LONG
1) Глобальные переменные для межзадачной работы:
const int BUTTON_PIN = 45;

volatile bool rawState = true;           // "сырое" состояние (true=отпущена)
volatile uint32_t isrTime = 0;           // millis() при срабатывании ISR
volatile bool state
Привожу надёжную и рекомендованную схему работы кнопки для ESP32 (и S3), которую используют в промышленности:

ISR только для фиксации факта изменения и записи millis() (или ESP.getCycleCount() для особо чувствительных задач).
Вся бизнес-логика дебаунса, двойного/длинного клика ведётся в отдельной задаче по данным из ISR.
Обязательно работа с Volatile, никаких тяжёлых вычислений/Serial в ISR.
Пример (по-настоящему устойчивая кнопка через ISR + задачу)
1. Глобальные флаги

volatile bool btn_event = false;           // факт события (любого фронта)
volatile uint8_t btn_raw = 1;              // последнее физическое состояние
volatile uint32_t btn_event_ms = 0;        // время события в миллисекундах
2. ISR (для обоих фронтов, максимально быстрый!)

const int BUTTON_PIN = 45;

void IRAM_ATTR onButtonChangeISR() {
    btn_raw = digitalRead(BUTTON_PIN);             // 0=нажата, 1=отпущена (INPUT_PULLUP)
    btn_event_ms = millis();                       // если нужна сверхточность — esp_timer_get_time()/1000
    btn_event = true;                              // поставить флаг для задачи
}
3. Задача-поллинг (ButtonTask):

void ButtonTask(void* pv) {
    const uint16_t DEBOUNCE_MS   = 35;
    const uint16_t DOUBLE_WIN_MS = 350;
    const uint16_t LONG_MS       = 2000;

    bool stableState = true;
    bool lastStable  = true;
    uint32_t lastChangeMs = millis();
    uint32_t pressStartMs = 0;

    bool waitingSecond = false;
    uint32_t secondWinEndMs = 0;
    bool longReported = false;

    TickType_t tick = pdMS_TO_TICKS(5);

    for(;;) {
        static uint8_t prev_raw = 1;
        bool curr_event = false;
        uint8_t curr_raw = 1;
        uint32_t curr_evt_ms = 0;

        // "Засекаем" событие из ISR (атомарно)
        if (btn_event) {
            curr_event = true;
            btn_event = false;
            curr_raw = btn_raw;
            curr_evt_ms = btn_event_ms;
        }

        uint32_t now = millis();
        // Дребезг: ждем DEBOUNCE_MS после любого нового события
        if (curr_event) {
            // здесь все фронты (нажатие/отпуск) попадают
            if (now - lastChangeMs >= DEBOUNCE_MS) {
                lastStable = stableState;
                stableState = (curr_raw == HIGH); // true = отпуск

                lastChangeMs = now;

                if (lastStable == true && stableState == false) {
                    // Отпуск -> нажатие
                    pressStartMs = now;
                    longReported = false;
                } else if (lastStable == false && stableState == true) {
                    // Нажатие -> отпуск
                    uint32_t dur = now - pressStartMs;
                    if (dur >= LONG_MS) {
                        if (!longReported)
                            AuxData.new_buttton_M = 3; // LONG
                        waitingSecond = false;
                    } else {
                        if (waitingSecond) {
                            AuxData.new_buttton_M = 2; // DOUBLE_SHORT
                            waitingSecond = false;
                        } else {
                            waitingSecond = true;
                            secondWinEndMs = now + DOUBLE_WIN_MS;
                        }
                    }
                }
            }
        } else {
            // нет физ. события — следим за long (пока нажата)
            if (stableState == false && !longReported && (now - pressStartMs >= LONG_MS)) {
                longReported = true;
                AuxData.new_buttton_M = 3; // LONG
                waitingSecond = false;
            }
        }
        // Окно второго клика закрылось — это SHORT
        if (waitingSecond && ((int32_t)(now - secondWinEndMs) >= 0)) {
            waitingSecond = false;
            AuxData.new_buttton_M = 1;  // SHORT
        }
        vTaskDelay(tick);
    }
}
4. В setup:

pinMode(BUTTON_PIN, INPUT_PULLUP);
attachInterrupt(digitalPinToInterrupt(BUTTON_PIN), onButtonChangeISR, CHANGE);
xTaskCreatePinnedToCore(ButtonTask, "BtnTask", 4096, NULL, 1, NULL, 1);
Почему теперь это стабильно
ISR максимально простой: только снимок входа и временной метки, никаких таймаутов и расчётов.
Дребезг, long/double обработка только в задаче-поллере.
Гарантирована предсказуемость событий и отсутствие потери двойных кликов из-за волн дребезга.
Широко используется в промышленности (сокращение ложных двойных срабаты
Отлично!
Оптимальный и надежный способ:

ISR фиксирует только сырое событие изменения кнопки (HIGH/LOW) и ставит флаг "были изменения" + фиксирует timestamp и новое состояние.
ВСЯ обработка таймингов: дебаунс, распознавание двойного/долгого/короткого клика — ТОЛЬКО в задаче ButtonTask (никаких длинных циклов, millis и счётчиков в ISR!).
Как сделать (пример, отработанный на ESP32):
1. Глобальные переменные/структура для событий
(volatile, чтобы и ISR, и задача могли читать)

typedef struct {
  volatile bool event;        // был фронт/спад (по CHANGE, ISR ставит true)
  volatile uint32_t timestamp;// millis ISR
  volatile bool state;        // HIGH (отпущена) / LOW (нажата)
} ButtonEvent_t;

ButtonEvent_t btnEvt = { false, 0, true };
const int BUTTON_PIN = 45;
2. ISR — только фиксирует факт события и время
IRAM_ATTR void onButtonChange() {
  btnEvt.event = true;
  btnEvt.timestamp = millis(); // на ESP32 millis в ISR годится (три линии времени); если глючит — используйте xTaskGetTickCountFromISR()
  btnEvt.state = digitalRead(BUTTON_PIN);
}
3. Задача обработки (ButtonTask) — вся логика таймингов
void ButtonTask(void* pvParameters)
{
  const uint16_t DEBOUNCE_MS      = 35;
  const uint16_t DOUBLE_WIN_MS    = 350;
  const uint16_t LONG_PRESS_MS    = 2000;

  bool lastStable = true;
  bool stable     = true;
  uint32_t lastChangeMs = 0;
  uint32_t pressStartMs = 0;

  bool waitingSecond = false;
  uint32_t secondWinEndMs = 0;

  bool longReported = false;
  volatile bool event;
  volatile uint32_t ts;
  volatile bool state;

  const TickType_t tick = pdMS_TO_TICKS(5);

  for (;;)
  {
    // 1. Смотрим, был ли фронт/спад (флаг в ISR)
    event = btnEvt.event;
    ts    = btnEvt.timestamp;
    state = btnEvt.state;

    if (event) {
      // сбрасываем флаг (atomic, но на ESP32 это ok)
      btnEvt.event = false;
      // Обработка антидребезга (таймер)
      if (millis() - lastChangeMs >= DEBOUNCE_MS) {
        lastChangeMs = millis();
        lastStable = stable;
        stable = (state == HIGH); // true - отпущена (INPUT_PULLUP)
        // Переход отпущена->нажата
        if (lastStable && !stable) {
          pressStartMs = millis();
          longReported = false;
        }
        // Переход нажата->отпущена
        else if (!lastStable && stable)
        {
          uint32_t dur = millis() - pressStartMs;
          if (dur >= LONG_PRESS_MS) {
            if (!longReported) AuxData.new_buttton_M = 3;
            waitingSecond = false;
          } else {
            if (waitingSecond) {
              AuxData.new_buttton_M = 2;
              waitingSecond = false;
            } else {
              waitingSecond = true;
              secondWinEndMs = millis() + DOUBLE_WIN_MS;
            }
          }
        }
      }
    }

    // Если кнопка удерживается — возможен LONG по времени
    if (!stable && !longReported && (millis() - pressStartMs >= LONG_PRESS_MS)) {
      longReported = true;
      AuxData.new_buttton_M = 3;
      waitingSecond = false;
    }

    // Окно двойного клика, если прошло — это одиночный клик (SHORT)
    if (waitingSecond && (int32_t)(millis() - secondWinEndMs) >= 0) {
      waitingSecond = false;
      AuxData.new_buttton_M = 1;
    }

    vTaskDelay(tick);
  }
}
4. В setup:
pinMode(BUTTON_PIN, INPUT_PULLUP);
attachInterrupt(digitalPinToInterrupt(BUTTON_PIN), onButtonChange, CHANGE);
xTaskCreatePinnedToCore(ButtonTask, "BtnTask", 4096, NULL, 1, &TaskHandle, 1);
Кратко:
ISR только фиксирует event/timestamp/state (без долгих вычислений!).
ВСЁ разбираем в задаче каждые 5–10 мс: дебаунс, длинное нажатие, двойное, короткое.
Выставляем только один new_buttton_M на событие (1—short, 2—double, 3—long).
Мгновенная отправка — отдельной задачей, как раньше (buttonMTask).
Такой вариант даст моментальную реакцию (ISR дергает event), но никогда не приведет к гонке или дублям.
Очень хорошо работает с дребезгом, быстрыми и долгими кликами, исключает ложные двойные по дребезгу или стекинг LONG/SHORT.

Будут вопросы — помогу доработать под нужную частоту/логики (triple-click и т.п.)