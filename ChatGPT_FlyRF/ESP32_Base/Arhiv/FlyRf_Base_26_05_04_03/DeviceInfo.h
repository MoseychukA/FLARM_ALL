/*
  Модуль DeviceInfo.h
  Назначение:
  - Общие определения, связанные с идентификатором устройства,
    режимами работы и состоянием нашего самолета.

  Что содержит файл:
  - Объявления функций получения Chip ID и версии программы.
  - Константы режимов работы устройства.
  - Структуру LocalAircraftState для параметров нашего самолета.
*/

#pragma once
#include <Arduino.h>
#include <time.h>

uint32_t DevID_Mapper(uint32_t id);
uint32_t getChipId();
void DeviceInfo_setProgramVersion(const String& version);
const String& DeviceInfo_programVersion();
String DeviceInfo_programVersionFromFile(const char* filePath);
String DeviceInfo_chipIdHex();

#define IMPUT_COORD_NONE   0U
#define IMPUT_COORD_MANUAL 1U
#define IMPUT_COORD_GNSS   2U

#define FLYRF_MODE_NORMAL      0U
#define FLYRF_MODE_TXRX_TEST1  1U
#define FLYRF_MODE_TXRX_TEST2  2U
#define FLYRF_MODE_TXRX_TEST3  3U
#define FLYRF_MODE_TXRX_TEST4  4U
#define FLYRF_MODE_TXRX_TEST_MAX FLYRF_MODE_TXRX_TEST4
#define FLYRF_MODE_TXRX_TEST5  5U /* obsolete, reserved for compatibility */

#define DEVICE_MODE_NORMAL FLYRF_MODE_NORMAL
#define DEVICE_MODE_TEST   FLYRF_MODE_TXRX_TEST1

#define TEST_MODE_STATIC    0U
#define TEST_MODE_RESERVED1 1U
#define TEST_MODE_RESERVED2 2U

//------------------------------------------------------------------------------
// Назначение функции: Вычисляет fly радиоканал режим uses local coordinates по текущим данным проекта и возвращает готовый результат.
// Локальные переменные: явные локальные переменные не используются; функция работает через параметры, глобальные структуры и результаты вызовов.
//------------------------------------------------------------------------------
static inline bool FlyRfMode_usesLocalCoordinates(uint8_t mode)
{
    return mode != FLYRF_MODE_NORMAL;
}

struct LocalAircraftState
{
    uint32_t addr;  // Параметр конфигурации: хранит выбранный режим, адрес, настройки модуля или пользовательское значение.
    int squawk;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    char callsign[8];  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    time_t timestamp;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    float altitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float pressure_altitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float course;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float speed;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    int vert_rate;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    float latitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float longitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float old_latitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float old_longitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float local_latitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float local_longitude;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    float geoid_separation;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    uint16_t hdop;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
    uint8_t aircraft_type;  // Навигационный или геометрический параметр: координаты, угол, дальность, размер или положение объекта.
    int16_t rp2040_gain;  // Рабочая переменная модуля: хранит промежуточное значение, используемое в текущей логике файла.
};

extern LocalAircraftState ThisAircraft;  // Параметр радиоканала или протокола: описывает частоту, мощность, профиль, режим передачи или текущее состояние RF.
