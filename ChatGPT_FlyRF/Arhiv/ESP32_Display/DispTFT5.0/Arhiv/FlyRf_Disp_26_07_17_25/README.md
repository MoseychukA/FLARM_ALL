# FlyRf_Disp_26_07_17_25

Исходная база: `FlyRf_Disp_26_05_18_01`. Версия проекта: `FlyRf_Disp_26_07_17_25`.

## Изменения версии 26_07_17_25

- выполнен возврат к последнему варианту с видимым изображением;
- используется отдельный полный framebuffer `800x480 RGB565` в PSRAM;
- кадр передаётся через `esp_lcd_panel_draw_bitmap()`;
- bounce-буферы полностью отключены;
- прямой framebuffer драйвера и ручная синхронизация кэша не применяются;
- проект проверяется с ESP32 Arduino `3.3.10`, ESP-IDF `5.5.4` и TFT_eSPI `2.5.43`;
- PCLK оставлен `18 МГц`, Wi-Fi и Web-интерфейс включены.

## Изменения версии 26_07_17_22

- проект переведён на пакет ESP32 Arduino `3.3.10`;
- включены два внутренних DMA bounce-буфера по `40 строк` каждый;
- размер DMA burst установлен `64 байта`;
- PCLK оставлен `18 МГц` для первого контрольного испытания;
- Wi-Fi и Web-интерфейс остаются включёнными;
- Arduino-приложение оставлено на Core 1.

## Изменения версии 26_07_17_21

- для устойчивой совместной работы RGB DMA и Wi-Fi частота PCLK снижена с 24 до 18 МГц;
- поток чтения RGB565 из PSRAM снижен с 48 до 36 МБ/с;
- расчётная кадровая частота составляет около 33 Гц;
- включён энергосберегающий режим Wi-Fi через `WiFi.setSleep(true)`;
- Wi-Fi и Web-интерфейс остаются включёнными;
- включены штатные `WiFi_setup()` и `WiFi_loop()`;
- включены штатные `Web_setup()` и `Web_loop()`;
- зафиксированная отрисовка RGB-экрана и его геометрия оставлены без изменений;
- напряжение и ток сдвинуты вправо на 15 пикселей;
- колонка `LATITUDE` сдвинута вправо на 10 пикселей;
- нижний GPS-блок сдвинут вправо на 15 пикселей;
- время поднято на базовую линию `y=32`, совпадающую с уровнем символа `N`;
- шрифт времени увеличен с 28 до 36 пикселей;
- GPS-блок справа выровнен по левому краю `x=600` и опущен до общей нижней линии `y=475`;
- заголовок `LONGITUDE` и его данные сдвинуты вправо на 10 пикселей;
- крайняя колонка `SIG` скорректирована для одинаковых видимых отступов слева и справа;
- нижний левый блок восстановлен по исходнику: `LoRa Tx`, `LoRa Rx`, `LoRa RF`, `LoRa RSSI`;
- нижний правый блок восстановлен по исходнику: `GPS Sat`, `GPS Lat`, `GPS Lon`;
- оба нижних блока опущены ещё на 12 пикселей, правый блок сдвинут влево до `x=780`;
- время в верхнем левом углу перенесено к `x=8`;
- расстояние между колонками `LATITUDE` и `LONGITUDE` увеличено с 90 до 125 пикселей;
- нижний левый служебный блок перенесён к левому краю экрана;
- нижний правый блок выровнен правым краем относительно `x=792`;
- цвет обоих нижних блоков сделан немного темнее основного текста;
- девять колонок таблицы равномерно распределены по всей ширине экрана;
- разделительная линия таблицы продлена от `x=8` до `x=792`;
- обозначения сторон света `N`, `E`, `S`, `W` увеличены с 18 до 28 пикселей;
- увеличенные символы сдвинуты внутрь рабочей области и полностью помещаются между левой и правой границами экрана;
- удалена отсутствующая в исходном проекте строка `ALT SPEED COURSE`;
- предупреждение потери связи заменено на крупную надпись `НЕТ СВЯЗИ С БАЗОЙ` по центру экрана;
- шрифт таблицы и нижних данных увеличен с 14 до 16 пикселей;
- нижние служебные данные опущены к нижнему краю экрана;
- центральная вертикальная линия собственного самолёта сделана утолщённой и чисто белой;
- напряжение, ток, батарея и процент заряда выровнены по одной строке без наложений;
- обычные надписи рабочего экрана сделаны белыми;
- окружность радара, внешние риски и обозначения сторон света сделаны ярче;
- таблица целей приведена к исходному составу из девяти полей: ICAO, SQUAWK, FLIGHT, ALT, SPEED, COURSE, LATITUDE, LONGITUDE и SIG;
- для таблицы добавлен компактный сглаженный шрифт 14 пикселей;
- после вывода рабочего кадра прекращена повторная подготовка данных дисплея, чтобы снизить нагрузку на процессор и PSRAM и повысить стабильность изображения.

В этой версии встроен новый дисплей `GL050001C0-40` для модуля `ESP32-S3-WROOM-1-N16R8`.

## Что изменено

- Добавлен RGB-драйвер `GL050001C0_40_Display.*` на базе `esp_lcd`.
- Сохранены проверенные параметры развертки AMT630A `800x480`, но PCLK снижен до `18 MHz` для диагностической проверки стабильности.
- При неизменных интервалах развертки частота кадров составляет около `33 Hz`.
- RGB DMA использует один framebuffer в PSRAM.
- Для ESP32 Arduino core 3.3.10 включены два внутренних bounce-буфера высотой `40 строк` каждый.
- Отрисовка идёт через полный framebuffer `800x480 RGB565` в PSRAM.
- Старый вывод через `TFT_eSPI` не запускается при `PROJECT_USE_GL050001C0_40 = 1`.
- В тестовом режиме однократно выводится стартовый экран, повторяющий композицию исходного проекта.
- Все надписи стартового экрана переведены на сглаженные растровые шрифты Arial.
- Полутоновые края букв смешиваются с фоном в формате RGB565.
- Русские надписи не зависят от поддержки кириллицы библиотекой дисплея.
- Рабочая страница FlyRF включается через три секунды после заставки.
- Код вывода времени, состояния базы, питания INA219, LoRa, RS485, GPS и параметров собственного борта сохранён без изменений.
- Добавлена таблица целей и радар с диапазонами 2, 5 и 10 км.
- Восстановлена геометрия исходного экрана `480x320` с единым масштабом `1,5`.
- Рабочая область имеет размер `720x480` и расположена по центру панели с полями по `40 px`.
- Центр радара перенесён в `(400, 360)`, внешний радиус увеличен до `350 px`.
- Рабочий кадр отправляется один раз после стартовой заставки и затем фиксируется.
- Убраны активные старые назначения `GPIO4` и `GPIO7`, так как эти GPIO заняты RGB-шиной дисплея.

## Важно

Это диагностическая версия для отдельной проверки геометрии и стабильности. Динамическое обновление рабочего экрана временно отключено.

Снижение PCLK и, при наличии в ESP32 core, bounce-буфер предназначены для устранения повторяющихся горизонтальных фрагментов при совместной работе дисплея, Wi-Fi и PSRAM. Геометрия рабочего экрана относительно версии 05 не изменена.

## Диагностический режим версии 12

- Установлен `PROJECT_GL050001C0_40_STATIC_IMAGE = 0`.
- Включены RS485, обработка данных и однократный вывод рабочей страницы.
- Wi-Fi и Web-интерфейс включены флагом `PROJECT_DIAGNOSTIC_DISABLE_WIFI_WEB = 0`.
- PCLK и геометрия оставлены такими же, как в стабильной версии 07.

Эта версия проверяет совместную работу зафиксированного RGB-кадра, RS485, Wi-Fi и Web-интерфейса.

## Исправление геометрии

- Внешняя окружность радара удалена: в исходном проекте её нет.
- Восстановлены 36 рисок компаса через 10 градусов; каждая третья риска имеет увеличенную длину.
- Внутреннее кольцо дальности сохранено с радиусом `180 px`, соответствующим исходным `120 px` при масштабе 1,5.
- Красная рамка относится к метке диапазона и имеет координаты `(364,453)` и размер `84x26 px`.
- Диапазон по умолчанию восстановлен на `32 km`, как на исходном изображении.
- Внешний предел размещения целей составляет `64 km`, то есть вдвое больше значения внутреннего кольца.
- Проекция целей повторяет исходную кусочно-линейную шкалу: `0–32 km` занимают радиус `0–180 px`, а `32–64 km` — `180–336 px`.
- Большая рамка потери связи убрана; состояние связи остаётся в верхней строке.

## Оформление версии 12

- Удалена серая заливка верхней строки и таблицы целей.
- Удалены две голубые горизонтальные полосы-разделители.
- Весь рабочий экран использует единый тёмный фон.
- С заставки удалена служебная оранжевая надпись `POWER`, перекрывавшая номер версии.

## Тексты и символы версии 12

- Исправлено построение масок `SmoothRuntimeFonts.h`: глифы теперь рисуются относительно базовой линии `anchor="ls"`.
- Восстановлен вывод времени, состояния базы, параметров движения и питания.
- Восстановлены заголовки и строки таблицы целей.
- Восстановлены подписи диапазона, сторон света, LoRa, RS485 и GPS.
- Все линии символа собственного самолёта принудительно рисуются чистым белым цветом RGB565.

После прошивки необходимо проверить устойчивость рабочего кадра при активном Wi-Fi и обращении к Web-интерфейсу.



